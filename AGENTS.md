# Researcher Context

This repository is the durable context lineage for Skillfoundry’s researcher.
Treat `memory/`, promoted `bundles/`, and explicitly reviewed state records as
authoritative Git-governed evidence. Keep raw run state non-canonical. Preserve
the distinction between internal findings, external evidence, assumptions,
probes, and decisions. The LCI outreach lane remains parked unless a successor
decision explicitly resumes it. Run `make check` before committing.

Read `CURRENT_STATE.md`, `memory/mission.md`, the pinned paths in
`skillfoundry.toml`, and `docs/architecture.md`.
