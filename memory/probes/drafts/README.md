# Probe Drafts

Use this directory for draft probe shapes before they become live commercial work.

Every serious draft should declare:

- the `assumption_id` it is trying to falsify
- the intended `probe_type`
- the target evidence class
- the minimum evidence quality needed to continue

Do not treat a draft as a live probe until the valuation-side venture loop accepts it.
